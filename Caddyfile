# # === Variante A: noch keine Domain, nur Server-IP / Hostname ===
# :80 {
#   encode gzip
#   @static {
#     path /static/* /favicon.ico /robots.txt
#   }
#   handle @static {
#     header Cache-Control "public, max-age=31536000, immutable"
#     file_server
#   }
#   handle {
#     reverse_proxy app:8000
#   }
# }

# === Variante B (später aktivieren): echte Domain mit Auto-HTTPS ===
HTTP → App (optional, für direkten IP-Zugriff)
:80 {
    encode gzip
    reverse_proxy app:8000
}

# Domain mit Auto-HTTPS
jp-winterhochzeit.de, www.jp-winterhochzeit.de {
    encode gzip
    reverse_proxy app:8000
}
